/**
 * Supabase Free — public client config only.
 * Fill in Project URL + anon (public) key from:
 * Supabase Dashboard → Project Settings → API
 *
 * NEVER put the service_role / secret key here.
 */
(function (global) {
  global.SUPABASE_CONFIG = {
    url: "",
    anonKey: ""
  };
})(window);
